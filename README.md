# password-fatigue-MINIproject

---

### 🚀 คำสั่งรันโปรเจกต์ (Docker Compose)

เปิด Terminal ที่โฟลเดอร์หลักของโปรเจกต์ (โฟลเดอร์ `password`) แล้วใช้คำสั่งนี้:

* **คำสั่งรันปกติ:**
```bash
docker compose up -d

```


* **คำสั่งรันแบบ Build ใหม่ (แนะนำ):** หากมีการแก้ไขไฟล์ `Dockerfile`, `package.json` หรือ `requirements.txt` ให้ใช้คำสั่งนี้เพื่อให้มั่นใจว่าข้อมูลล่าสุดถูกนำไปใช้
```bash
docker compose up -d --build

```

---

### 🛑 คำสั่งหยุดการทำงาน

หากต้องการปิดระบบทั้งหมดเพื่อพักเครื่อง ให้ใช้คำสั่งนี้:

```bash
docker compose down

```

---

### 🌐 ที่อยู่ (URL) สำหรับเข้าใช้งาน

หลังจากรันคำสั่งสำเร็จแล้ว สามารถตรวจสอบผลงานได้ที่นี่:

* **หน้าเว็บ (Frontend):** เข้าไปที่ [http://localhost:3210](https://www.google.com/search?q=http://localhost:3210)
* **ระบบหลังบ้าน (Backend API):** เข้าไปที่ [http://localhost:8000/docs](https://www.google.com/search?q=http://localhost:8000/docs) (เพื่อดูคู่มือ API)

---

### 🔍 วิธีเช็คว่าระบบรันอยู่จริงไหม?

1. **ดูใน VS Code:** สังเกตแถบสีน้ำเงินด้านล่าง หรือใช้คำสั่ง `docker ps` ใน Terminal เพื่อดูรายชื่อ Container ที่กำลังวิ่งอยู่
2. **ดูใน Docker Desktop:** หากติดตั้งโปรแกรมไว้ สามารถเปิดดูสถานะของตู้คอนเทนเนอร์ให้เป็นสีเขียว

> **ทริค:** ถ้าเผลอไปกดปิด VS Code หรือปิด Terminal ไปแล้ว แต่ไม่ได้สั่ง `docker compose down` ระบบจะยังคงทำงานอยู่เบื้องหลัง
